# Project metadata and build configuration has been moved to pyproject.toml (PEP 517/518).
# This file is kept only for backward compatibility with legacy build tools.
from setuptools import setup

setup()
